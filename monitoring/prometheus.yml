global:
  scrape_interval: 15s       # how often Prometheus polls targets
  evaluation_interval: 15s   # how often rules are evaluated

scrape_configs:
  - job_name: "heart-disease-api"
    static_configs:
      - targets: ["heart-disease-api:8000"]   # docker-compose service name + port
    metrics_path: "/metrics"
